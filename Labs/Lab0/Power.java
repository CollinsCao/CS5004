public class Power {
    public static void main(String[] args) {
        long result = 1L << 31;
        System.out.println(result);
    }

    /*
     * Differences between Java and Python:
     * 1. Java requires explicit type declarations, Python does not.
     * 2. Java code must be inside a class, Python code does not.
     * 3. Java needs a main method to run, Python runs top-down.
     */
}